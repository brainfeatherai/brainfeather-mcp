Import durable facts already written in this repository.

Call the Brainfeather `onboard_project` tool now. It reads AGENTS.md, CLAUDE.md, .cursorrules, and .cursor/rules, then saves those lines as user-stated memories. It does not invent stack guesses. After it returns, summarize what was saved versus already known.
